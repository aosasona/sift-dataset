# Sift dataset

The model was trained using the CreateML application with 70 iterations and Transfer Learning with BERT Embeddings.

# Dataset

The original dataset is located at `./dataset.json`, and the processed version is at `./dataset.csv`.
